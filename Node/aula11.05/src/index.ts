let nome: string | null = "";
let num: number = 0;
let bole: boolean = false;

console.log(nome);

let listaNomes: Array<string | number | boolean> = [];

let aluno = {
    nome: "João",
    aprovado: true,
    idade: null
    
}

function imprimeNome(nome:string, aprovado:boolean, idade:number) {
    console.log(`Nome do aluno é ${nome}`);
    console.log(`Situação do aluno ${aprovado}`);
    console.log(`idade do aluno é: ${idade}`);
}





